# 项目结构规则

## 🎯 目的

本文件定义项目的目录结构规范，确保AI团队和人类开发者都能正确组织代码。

**所有新文件必须遵循本规范，不得将文件随意放置到根目录！**

## 📁 标准目录结构

```
项目根目录/
│
├── 📄 项目元数据（允许在根目录）
│   ├── package.json           # ✅ 包配置
│   ├── pnpm-workspace.yaml    # ✅ 工作区配置
│   ├── tsconfig.json          # ✅ TypeScript配置
│   ├── README.md              # ✅ 项目说明
│   └── CLAUDE.md              # ✅ AI规则
│
├── 📦 源代码（必须放在 src/）
│   ├── src/
│   │   ├── core/              # 核心业务逻辑
│   │   │   ├── entities/      # 业务实体
│   │   │   ├── value-objects/ # 值对象
│   │   │   └── aggregates/    # 聚合根
│   │   │
│   │   ├── services/          # 服务层
│   │   │   ├── user-service/
│   │   │   ├── order-service/
│   │   │   └── ...
│   │   │
│   │   ├── utils/             # 工具函数
│   │   │   ├── format.ts
│   │   │   ├── validators.ts
│   │   │   └── helpers.ts
│   │   │
│   │   ├── config/            # 配置
│   │   │   ├── database.ts
│   │   │   ├── cache.ts
│   │   │   └── app.ts
│   │   │
│   │   ├── types/             # 类型定义
│   │   │   ├── user.types.ts
│   │   │   ├── order.types.ts
│   │   │   └── common.types.ts
│   │   │
│   │   └── api/               # API相关
│   │       ├── routes/
│   │       ├── middleware/
│   │       └── controllers/
│   │
│   ├── components/            # UI组件
│   │   ├── common/            # 通用组件
│   │   ├── layouts/           # 布局组件
│   │   └── pages/             # 页面组件
│   │
│   └── extensions/            # 扩展/插件
│       ├── channel-plugins/
│       └── feature-plugins/
│
├── 🧪 测试（必须放在 tests/）
│   └── tests/
│       ├── unit/              # 单元测试
│       │   ├── services/
│       │   ├── utils/
│       │   └── core/
│       │
│       ├── integration/       # 集成测试
│       │   ├── api/
│       │   └── services/
│       │
│       └── e2e/               # 端到端测试
│           └── flows/
│
├── 📚 文档（必须放在 docs/）
│   └── docs/
│       ├── guides/            # 使用指南
│       ├── architecture/      # 架构文档
│       ├── api/               # API文档
│       └── changelog/         # 更新日志
│
├── 🛠️ 脚本（必须放在 scripts/）
│   └── scripts/
│       ├── build/             # 构建脚本
│       ├── deploy/            # 部署脚本
│       ├── maintenance/       # 维护脚本
│       └── tools/             # 工具脚本
│
├── 🎨 静态资源
│   └── assets/
│       ├── images/            # 图片
│       ├── icons/             # 图标
│       ├── styles/            # 样式
│       └── templates/         # 模板
│
├── 📦 包和模块
│   ├── packages/              # 可复用的包
│   │   ├── sdk/
│   │   └── shared/
│   │
│   └── apps/                  # 独立应用
│       ├── web/
│       ├── mobile/
│       └── desktop/
│
├── ⚙️ 工具配置（允许在根目录）
│   ├── .gitignore
│   ├── .eslintrc.js
│   ├── .prettierrc
│   ├── .editorconfig
│   └── .env.example
│
└── 📦 其他目录
    ├── node_modules/          # 依赖（自动生成）
    ├── dist/                  # 构建输出（自动生成）
    ├── skills/                # AI技能
    ├── i18n/                  # 国际化
    └── vendor/                # 第三方依赖
```

## 🚫 禁止行为

### ❌ 绝对不要这样做

```bash
# ❌ 禁止：将代码文件放到根目录
touch my-module.ts          # 错误！
touch utils.js              # 错误！
touch service.py            # 错误！
touch config.json           # 错误！

# ❌ 禁止：创建新的顶级目录
mkdir my-code               # 错误！
mkdir my-tests              # 错误！
mkdir my-docs               # 错误！

# ❌ 禁止：将测试文件与源码混放
touch src/service.test.ts   # 错误！

# ❌ 禁止：将文档放到根目录
touch HOW_TO_USE.md         # 错误！
touch API_NOTES.txt         # 错误！

# ❌ 禁止：将脚本放到根目录（除非是安装脚本）
touch deploy.sh             # 错误！
touch build.js              # 错误！
```

## ✅ 正确做法

### 示例1：创建新的服务模块

```bash
# ✅ 正确：在 src/services/ 下创建
mkdir -p src/services/user-management

# 创建服务文件
touch src/services/user-management/index.ts
touch src/services/user-management/user-service.ts
touch src/services/user-management/user-types.ts
touch src/services/user-management/user-validator.ts

# ✅ 正确：在 tests/ 下创建测试
mkdir -p tests/unit/services/user-management
touch tests/unit/services/user-management/user-service.test.ts

# ✅ 正确：在 docs/ 下创建文档
touch docs/guides/user-management.md

# ✅ 最终结构
src/services/user-management/
├── index.ts
├── user-service.ts
├── user-types.ts
└── user-validator.ts

tests/unit/services/user-management/
└── user-service.test.ts
```

### 示例2：添加工具函数

```bash
# ✅ 正确：放到 src/utils/
touch src/utils/date-formatter.ts
touch src/utils/string-helpers.ts
touch src/utils/http-utils.ts

# ❌ 错误：不要放到根目录
touch date-formatter.ts     # 错误！
touch utils.js              # 错误！
```

### 示例3：添加配置

```bash
# ✅ 正确：放到 src/config/
touch src/config/database.ts
touch src/config/redis.ts
touch src/config/app.ts

# ❌ 错误：不要散落在根目录
touch database.json         # 错误！
touch redis.js              # 错误！
```

### 示例4：创建测试

```bash
# ✅ 正确：放到 tests/ 目录
touch tests/unit/utils/date-formatter.test.ts
touch tests/integration/api/users.test.ts
touch tests/e2e/flows/login.test.ts

# ❌ 错误：不要和源码混在一起
touch src/utils/date-formatter.test.ts  # 错误！
```

## 📍 文件位置快速参考

### 代码文件

| 文件类型 | 目录 | 示例 |
|---------|------|------|
| 业务服务 | `src/services/` | `src/services/order-service.ts` |
| 工具函数 | `src/utils/` | `src/utils/format.ts` |
| 配置 | `src/config/` | `src/config/database.ts` |
| 类型定义 | `src/types/` | `src/types/user.ts` |
| 实体 | `src/core/entities/` | `src/core/entities/user.ts` |
| API路由 | `src/api/routes/` | `src/api/routes/users.ts` |
| 中间件 | `src/api/middleware/` | `src/api/middleware/auth.ts` |
| 控制器 | `src/api/controllers/` | `src/api/controllers/user-controller.ts` |
| UI组件 | `components/` | `components/UserProfile.tsx` |

### 测试文件

| 测试类型 | 目录 | 示例 |
|---------|------|------|
| 单元测试 | `tests/unit/` | `tests/unit/services/user-service.test.ts` |
| 集成测试 | `tests/integration/` | `tests/integration/api/users.test.ts` |
| E2E测试 | `tests/e2e/` | `tests/e2e/flows/login.test.ts` |

### 文档

| 文档类型 | 目录 | 示例 |
|---------|------|------|
| 使用指南 | `docs/guides/` | `docs/guides/setup.md` |
| API文档 | `docs/api/` | `docs/api/users.md` |
| 架构文档 | `docs/architecture/` | `docs/architecture/overview.md` |
| 更新日志 | `docs/changelog/` | `docs/changelog/v1.0.0.md` |

### 脚本

| 脚本类型 | 目录 | 示例 |
|---------|------|------|
| 构建脚本 | `scripts/build/` | `scripts/build/compile.sh` |
| 部署脚本 | `scripts/deploy/` | `scripts/deploy/run.sh` |
| 维护脚本 | `scripts/` | `scripts/cleanup.sh` |
| 工具脚本 | `scripts/tools/` | `scripts/tools/migrate.js` |

## 🏗️ 架构规则

### 分层架构

```
┌─────────────────────────┐
│   应用层 (Application)   │  ← 最上层
├─────────────────────────┤
│   服务层 (Services)      │  ← 可以依赖下面所有层
├─────────────────────────┤
│   核心层 (Core)          │  ← 只能依赖工具层
├─────────────────────────┤
│   工具层 (Utils)         │  ← 最底层，不能依赖其他层
└─────────────────────────┘
```

### 依赖方向规则

**规则1：高层可以依赖低层**

```typescript
// ✅ 正确：服务层依赖核心层和工具层
import { User } from '../core/user';           // 核心层
import { validateEmail } from '../utils/validators'; // 工具层

export class UserService {
  async createUser(data: CreateUserInput) {
    validateEmail(data.email);
    const user = new User(data);
    return user.save();
  }
}
```

**规则2：低层不能依赖高层**

```typescript
// ❌ 错误：核心层不能依赖服务层
import { UserService } from '../services/user-service'; // 禁止！

export class User {
  // 核心实体不应该依赖服务
}
```

**规则3：同层可以互相依赖**

```typescript
// ✅ 正确：同层服务可以互相依赖
import { EmailService } from './email-service';

export class NotificationService {
  async sendNotification(userId: string) {
    const email = await this.getUserEmail(userId);
    await this.emailService.send(email, 'Notification');
  }
}
```

## 🎯 创建文件的决策流程

当需要创建新文件时，按以下流程决策：

```
1. 这个文件是什么类型？
   │
   ├─ 业务逻辑/服务？
   │  └─ 放到 src/services/<模块名>/
   │
   ├─ 工具函数？
   │  └─ 放到 src/utils/
   │
   ├─ 配置？
   │  └─ 放到 src/config/
   │
   ├─ 类型定义？
   │  └─ 放到 src/types/
   │
   ├─ 业务实体？
   │  └─ 放到 src/core/entities/
   │
   ├─ UI组件？
   │  └─ 放到 components/
   │
   ├─ 测试？
   │  ├─ 单元测试？ → tests/unit/对应路径
   │  ├─ 集成测试？ → tests/integration/对应路径
   │  └─ E2E测试？ → tests/e2e/对应路径
   │
   ├─ 文档？
   │  └─ 放到 docs/对应分类/
   │
   └─ 脚本？
      └─ 放到 scripts/对应分类/
```

## 📋 检查清单

在创建新文件前，必须确认：

- [ ] 已确认文件类型（服务、工具、配置等）
- [ ] 已确定正确的目录位置
- [ ] 检查是否已有类似的目录结构
- [ ] 遵循命名约定
- [ ] 不会破坏现有的项目结构
- [ ] 如果需要创建新目录，有充分理由

## 🛠️ 自动检查

项目提供了自动检查工具，确保遵守规范：

```bash
# 检查根目录是否整洁
pnpm check:root-directory

# 自动清理根目录
pnpm check:root-directory:fix

# 检查依赖版本一致性
pnpm ai:dependency-check

# 自动修复依赖问题
pnpm ai:dependency-fix
```

## 💡 最佳实践

### 1. 先规划，后创建

```bash
# 1. 查看现有结构
tree -L 3
ls -la src/services/

# 2. 规划新模块的位置
# 例如：创建订单服务 → src/services/order-service/

# 3. 创建目录结构
mkdir -p src/services/order-service
mkdir -p tests/unit/services/order-service

# 4. 创建文件
touch src/services/order-service/index.ts
touch src/services/order-service/order-service.ts
touch src/services/order-service/order-types.ts

# 5. 创建测试
touch tests/unit/services/order-service/order-service.test.ts
```

### 2. 保持一致性

参考现有模块的组织方式：

```bash
# 查看现有服务是如何组织的
tree src/services/user-service/
tree src/services/email-service/

# 按照相同的模式创建新服务
mkdir -p src/services/new-service
# 复制相同的文件结构
```

### 3. 模块化设计

```bash
# ✅ 正确：每个服务独立模块
src/services/
├── user-service/
│   ├── index.ts
│   ├── user-service.ts
│   ├── user-types.ts
│   └── user-validator.ts
├── order-service/
│   ├── index.ts
│   ├── order-service.ts
│   └── order-types.ts
└── email-service/
    ├── index.ts
    └── email-service.ts

# ❌ 错误：所有东西混在一起
src/
├── user-service.ts
├── order-service.ts
├── email-service.ts
├── user-types.ts
├── order-types.ts
└── utils.ts
```

## 📚 相关文档

- [AI开发规则](CLAUDE.md) - AI团队开发规范
- [Cursor规则](.cursorrules) - AI编辑器配置
- [依赖管理](skills/dependency-check/SKILL.md) - 依赖检查技能

## ⚠️ 重要提醒

**所有团队成员（包括AI）必须遵守本规范！**

违反规范会导致：
- ❌ 项目结构混乱
- ❌ 代码难以维护
- ❌ 协作困难
- ❌ 打包和构建失败

**遵守规范可以确保：**
- ✅ 项目结构清晰
- ✅ 代码易于理解
- ✅ 团队协作高效
- ✅ 长期可维护

---

**最后更新：2025-01**
**适用范围：所有新文件和模块**
