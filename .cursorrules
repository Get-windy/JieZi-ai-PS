# AI代码生成规则
# 适用于 Cursor、Windsurf、GitHub Copilot 等AI工具

## 🎯 角色定义

你是一个专业的软件工程师，正在帮助团队开发项目。**你必须严格遵守项目结构规范，不要自行决定文件存放位置。**

## 📁 项目结构规则

### 绝对禁止

❌ **不要将新文件直接创建在项目根目录**
❌ **不要创建新的顶级目录**
❌ **不要打乱现有的目录结构**

### 必须遵守

✅ **所有新代码文件必须放在正确的功能目录**
✅ **遵循现有的命名约定**
✅ **保持项目结构整洁**

## 📍 文件位置指南

### 创建新代码文件时

| 文件类型 | 应该放在 | 示例 |
|---------|---------|------|
| 业务逻辑/服务 | `src/services/` | `src/services/user-service.ts` |
| 工具函数 | `src/utils/` | `src/utils/format.ts` |
| 配置 | `src/config/` | `src/config/database.ts` |
| 类型定义 | `src/types/` | `src/types/user.ts` |
| 核心实体 | `src/core/` | `src/core/user.ts` |
| UI组件 | `components/` | `components/UserProfile.tsx` |
| API路由 | `src/api/routes/` | `src/api/routes/users.ts` |
| 中间件 | `src/api/middleware/` | `src/api/middleware/auth.ts` |

### 创建测试文件时

| 测试类型 | 应该放在 | 示例 |
|---------|---------|------|
| 单元测试 | `tests/unit/` | `tests/unit/services/user-service.test.ts` |
| 集成测试 | `tests/integration/` | `tests/integration/api/users.test.ts` |
| E2E测试 | `tests/e2e/` | `tests/e2e/flows/login.test.ts` |

### 创建文档时

| 文档类型 | 应该放在 | 示例 |
|---------|---------|------|
| 使用指南 | `docs/guides/` | `docs/guides/setup.md` |
| API文档 | `docs/api/` | `docs/api/users.md` |
| 架构文档 | `docs/architecture/` | `docs/architecture/overview.md` |

### 创建脚本时

| 脚本类型 | 应该放在 | 示例 |
|---------|---------|------|
| 构建脚本 | `scripts/build/` | `scripts/build/compile.sh` |
| 部署脚本 | `scripts/deploy/` | `scripts/deploy/run.sh` |
| 维护脚本 | `scripts/` | `scripts/cleanup.sh` |

## 🚫 常见错误示例

### 错误1：创建文件到根目录

```bash
# ❌ 错误 - 不要把文件放到根目录
touch my-module.ts
touch utils.js
touch config.json
touch test.js

# ✅ 正确 - 使用正确的目录
touch src/services/my-module.ts
touch src/utils/format.ts
touch src/config/app.ts
touch tests/unit/services/my-module.test.ts
```

### 错误2：混合不同类型的文件

```bash
# ❌ 错误 - 不要把测试和源码混在一起
src/services/user-service.ts
src/services/user-service.test.ts  # 错误！

# ✅ 正确 - 测试放在 tests/ 目录
src/services/user-service.ts
tests/unit/services/user-service.test.ts  # 正确！
```

### 错误3：配置文件散落各处

```bash
# ❌ 错误 - 不要到处放配置文件
/config.json
/settings.js
/database.yml

# ✅ 正确 - 统一放在 src/config/
src/config/app.ts
src/config/database.ts
src/config/cache.ts
```

## ✅ 正确做法示例

### 创建完整的服务模块

```bash
# 1. 创建服务目录（在 src/services/ 下）
mkdir -p src/services/user-management

# 2. 创建服务文件
touch src/services/user-management/index.ts
touch src/services/user-management/user-service.ts
touch src/services/user-management/user-types.ts
touch src/services/user-management/user-validator.ts

# 3. 创建对应的测试
mkdir -p tests/unit/services/user-management
touch tests/unit/services/user-management/user-service.test.ts
touch tests/unit/services/user-management/user-validator.test.ts

# 4. 如果需要API路由
touch src/api/routes/users.ts

# 5. 如果需要文档
touch docs/api/users.md
```

### 最终的正确结构

```
项目根目录/
├── src/
│   ├── services/
│   │   └── user-management/
│   │       ├── index.ts
│   │       ├── user-service.ts
│   │       ├── user-types.ts
│   │       └── user-validator.ts
│   ├── api/
│   │   └── routes/
│   │       └── users.ts
│   └── config/
│       └── database.ts
│
├── tests/
│   └── unit/
│       └── services/
│           └── user-management/
│               ├── user-service.test.ts
│               └── user-validator.test.ts
│
└── docs/
    └── api/
        └── users.md
```

## 🏗️ 架构规则

### 依赖方向（必须遵守）

```
应用层 (Application)
  ↓ 可以依赖
服务层 (Services)
  ↓ 可以依赖
核心层 (Core)
  ↓ 可以依赖
工具层 (Utils)
```

**关键规则：低层不能依赖高层**

```typescript
// ✅ 正确：服务可以依赖核心
import { User } from '../core/user';
import { validate } from '../utils/validator';

export class UserService {
  // 实现
}

// ❌ 错误：核心不能依赖服务
import { UserService } from '../services/user-service'; // 禁止！

export class User {
  // 不应该使用 UserService
}
```

## 📝 代码规范

### 命名约定

```typescript
// 文件名：kebab-case
user-service.ts
api-config.ts

// 类名：PascalCase
class UserService { }
class ApiController { }

// 函数/变量名：camelCase
function getUserInfo() { }
const userName = "John";

// 常量名：UPPER_SNAKE_CASE
const MAX_RETRIES = 3;
const API_BASE_URL = "https://api.example.com";

// 接口名：PascalCase，以 I 开头（可选）
interface IUser { }
interface ConfigOptions { }

// 类型名：PascalCase
type Status = 'active' | 'inactive';
type UserRole = 'admin' | 'user';
```

### TypeScript 规范

```typescript
// ✅ 使用明确的类型
const users: User[] = [];
function getUser(id: string): Promise<User> { }

// ✅ 使用接口定义契约
interface UserService {
  create(data: CreateUserInput): Promise<User>;
  findById(id: string): Promise<User | null>;
}

// ✅ 使用 async/await
async function processUser(id: string) {
  const user = await userRepository.findById(id);
  if (!user) throw new Error('User not found');
  return user;
}

// ❌ 避免使用 any
function process(data: any) { } // 不好！

// ✅ 使用联合类型和类型守卫
type Status = 'pending' | 'active' | 'completed';

function isActive(status: Status): boolean {
  return status === 'active';
}
```

## 🔧 开发工作流

### 创建新功能时

1. **规划结构** - 确定文件应该放在哪个目录
2. **创建目录** - 在正确的位置创建目录结构
3. **创建文件** - 按规范创建代码文件
4. **编写测试** - 在 tests/ 目录创建对应测试
5. **更新文档** - 在 docs/ 目录添加文档
6. **验证结构** - 运行 `tree` 或 `ls` 确认结构正确

### 提交前检查

```bash
# 1. 检查项目结构
pnpm check:root-directory

# 2. 检查依赖
pnpm ai:dependency-check

# 3. 运行测试
pnpm test

# 4. 代码格式化
pnpm lint
pnpm format
```

## 💡 决策树

当不确定文件应该放在哪里时，使用这个决策树：

```
这个文件是什么类型？
│
├─ 业务逻辑/服务？
│  └─ 放到 src/services/
│
├─ 工具函数？
│  └─ 放到 src/utils/
│
├─ 配置？
│  └─ 放到 src/config/
│
├─ 类型定义？
│  └─ 放到 src/types/
│
├─ UI组件？
│  └─ 放到 components/
│
├─ 测试？
│  ├─ 单元测试？ → tests/unit/
│  ├─ 集成测试？ → tests/integration/
│  └─ E2E测试？ → tests/e2e/
│
├─ 文档？
│  └─ 放到 docs/
│
└─ 脚本？
   └─ 放到 scripts/
```

## 🎓 学习资源

如果你对项目结构有疑问，应该：

1. 查看现有的目录结构
2. 参考类似模块的组织方式
3. 遵循团队的编码规范
4. 询问用户确认不确定的地方

## 📌 最重要的规则

**在创建任何新文件之前，你必须：**

1. ✅ 确认这个文件应该放在哪个目录
2. ✅ 检查是否已经有类似的目录结构
3. ✅ 遵循现有的命名约定
4. ✅ 确保不会破坏项目结构
5. ✅ 如果需要创建新目录，确保有充分的理由

**记住：一个整洁的项目结构对于长期维护和团队协作至关重要！**
